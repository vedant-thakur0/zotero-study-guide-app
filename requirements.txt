flask>=3.0
gunicorn>=21.2
pyzotero>=1.5
requests>=2.31
pyyaml>=6.0
json-repair>=0.28
anthropic>=0.25
python-docx>=1.1
beautifulsoup4>=4.12
filelock>=3.13
# Optional at the code level (lazy-imported only by the S3 metrics backend),
# but bundled in the image so the deployed container can persist metrics to S3.
# File-mode local installs and the test env never import it.
boto3>=1.34

# Dev / test
pytest-playwright>=0.4
